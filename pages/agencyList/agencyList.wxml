<view>
  <view>
    <view wx:for="{{agencyes}}" wx:key="{{item.id}}" bindtap="openAgency" data-cid="{{item.id}}">
    <image src='{{item.logo}}'></image>
    <text>{{item.agencyName}}</text>
    <view></view>
    <text>{{item.address}}</text>
    <view></view>
    <text>{{item.mobilephone}}</text>
  </view>
  </view>
</view>
